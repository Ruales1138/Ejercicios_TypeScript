// 1. Crear un array con 5 nombres de estudiantes y mostrar cada nombre en consola usando un ciclo.

let names: string[] = ['Tomas', 'Sara', 'Kevin', 'Juan', 'David']

for (let name of names) {
    console.log(name)
}

// 2. Imprimir cuantos estudiantes hay en el arreglo.

console.log('Numero de estudiantes: ' + names.length)